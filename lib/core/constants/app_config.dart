abstract class AppConfig {
  static const hasAcceptedRisksKey = 'has_accepted_risks';
  static const silenceModeEnabledKey = 'silence_mode_enabled';
  static const interactionModeKey = 'interaction_mode';
}
